apiVersion: v1
kind: Service
metadata:
  name: community-frontend-svc
  namespace: community
  labels:
    app: community-frontend
spec:
  type: ClusterIP
  selector:
    app: community-frontend
    tier: frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  