apiVersion: v1
kind: Service
metadata:
  name: community-backend-svc
  namespace: community
  labels:
    app: community-backend
spec:
  type: ClusterIP
  selector:
    app: community-backend
    tier: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP